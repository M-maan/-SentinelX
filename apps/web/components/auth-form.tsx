'use client'; import { FormEvent, useState } from 'react'; import { useRouter } from 'next/navigation'; import { authApi } from '../lib/auth.api'; import { useAuthStore } from '../lib/auth-store';
export function AuthForm({ mode }: { mode: 'login'|'register' }) { const router=useRouter(), setSession=useAuthStore(s=>s.setSession); const [error,setError]=useState(''),[busy,setBusy]=useState(false); const submit=async(e:FormEvent<HTMLFormElement>)=>{e.preventDefault();setError('');const f=new FormData(e.currentTarget);const password=String(f.get('password'));if(mode==='register'&&password.length<12)return setError('Password must be at least 12 characters.');setBusy(true);try{const r=await (mode==='login'?authApi.login({email:f.get('email'),password}):authApi.register({name:f.get('name'),email:f.get('email'),password,organizationName:f.get('organizationName'),industry:f.get('industry')||undefined}));setSession(r);router.push('/dashboard')}catch(e){setError(e instanceof Error?e.message:'Unable to continue')}finally{setBusy(false)}};return <form onSubmit={submit}>{mode==='register'&&<><div className="field"><label>Name</label><input name="name" required minLength={2}/></div><div className="field"><label>Organization</label><input name="organizationName" required minLength={2}/></div><div className="field"><label>Industry (optional)</label><input name="industry"/></div></>}<div className="field"><label>Email</label><input name="email" type="email" required/></div><div className="field"><label>Password</label><input name="password" type="password" required minLength={12}/></div>{error&&<p className="error">{error}</p>}<button className="button" disabled={busy}>{busy?'Please wait…':mode==='login'?'Sign in':'Create secure workspace'}</button></form> }
