import { MigrationInterface, QueryRunner } from 'typeorm';
export class InitialFoundation1720000000000 implements MigrationInterface { async up(q: QueryRunner): Promise<void> { await q.query('CREATE EXTENSION IF NOT EXISTS "pgcrypto"'); await q.query(`CREATE TYPE "users_role_enum" AS ENUM ('SUPER_ADMIN','SECURITY_ADMIN','ANALYST','VIEWER'); CREATE TABLE "organizations" ("id" uuid PRIMARY KEY DEFAULT gen_random_uuid(), "name" varchar(160) NOT NULL, "industry" varchar(120), "created_at" TIMESTAMPTZ NOT NULL DEFAULT now(), "updated_at" TIMESTAMPTZ NOT NULL DEFAULT now()); CREATE TABLE "users" ("id" uuid PRIMARY KEY DEFAULT gen_random_uuid(), "name" varchar(160) NOT NULL, "email" varchar(320) NOT NULL UNIQUE, "password_hash" varchar NOT NULL, "role" "users_role_enum" NOT NULL DEFAULT 'VIEWER', "organization_id" uuid REFERENCES "organizations"("id") ON DELETE SET NULL, "created_at" TIMESTAMPTZ NOT NULL DEFAULT now(), "updated_at" TIMESTAMPTZ NOT NULL DEFAULT now()); CREATE TABLE "refresh_tokens" ("id" uuid PRIMARY KEY DEFAULT gen_random_uuid(), "user_id" uuid NOT NULL REFERENCES "users"("id") ON DELETE CASCADE, "token" varchar NOT NULL UNIQUE, "expires_at" TIMESTAMPTZ NOT NULL, "created_at" TIMESTAMPTZ NOT NULL DEFAULT now()); CREATE TABLE "audit_logs" ("id" uuid PRIMARY KEY DEFAULT gen_random_uuid(), "user_id" uuid, "action" varchar(100) NOT NULL, "ip_address" varchar(64), "metadata" jsonb, "created_at" TIMESTAMPTZ NOT NULL DEFAULT now());`); } async down(q: QueryRunner): Promise<void> { await q.query('DROP TABLE "audit_logs"; DROP TABLE "refresh_tokens"; DROP TABLE "users"; DROP TABLE "organizations"; DROP TYPE "users_role_enum";'); } }
