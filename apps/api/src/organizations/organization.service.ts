import { Injectable, NotFoundException } from '@nestjs/common'; import { InjectRepository } from '@nestjs/typeorm'; import { Repository } from 'typeorm'; import { Organization } from '../database/entities/organization.entity'; import { CreateOrganizationDto } from './dto/organization.dto';
@Injectable() export class OrganizationService { constructor(@InjectRepository(Organization) private organizations: Repository<Organization>) {} async current(organizationId: string | null) { if (!organizationId) throw new NotFoundException('No organization is assigned to this user'); const organization = await this.organizations.findOne({ where: { id: organizationId } }); if (!organization) throw new NotFoundException('Organization not found'); return organization; } list() { return this.organizations.find({ order: { createdAt: 'DESC' } }); } create(dto: CreateOrganizationDto) { return this.organizations.save(this.organizations.create({ name: dto.name, industry: dto.industry ?? null })); } }
