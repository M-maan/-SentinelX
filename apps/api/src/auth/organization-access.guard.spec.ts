import { ForbiddenException } from '@nestjs/common'; import { OrganizationAccessGuard } from './organization-access.guard'; import { Role } from '../database/entities/user.entity';
describe('OrganizationAccessGuard', () => { const guard = new OrganizationAccessGuard(); const context = (user: unknown, params = {}) => ({ switchToHttp: () => ({ getRequest: () => ({ user, params, body: {} }) }) }) as any; it('allows a user to access their own organization', () => expect(guard.canActivate(context({ sub: 'u', role: Role.ANALYST, organizationId: 'org-a' }, { organizationId: 'org-a' }))).toBe(true)); it('blocks cross-organization access', () => expect(() => guard.canActivate(context({ sub: 'u', role: Role.ANALYST, organizationId: 'org-a' }, { organizationId: 'org-b' }))).toThrow(ForbiddenException)); it('allows Super Admin access', () => expect(guard.canActivate(context({ sub: 'u', role: Role.SUPER_ADMIN, organizationId: null }, { organizationId: 'org-b' }))).toBe(true)); });
