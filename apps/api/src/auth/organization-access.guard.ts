import { CanActivate, ExecutionContext, ForbiddenException, Injectable, UnauthorizedException } from '@nestjs/common'; import { Role } from '../database/entities/user.entity';
type Principal = { sub: string; role: Role; organizationId: string | null };
@Injectable() export class OrganizationAccessGuard implements CanActivate { canActivate(context: ExecutionContext) { const request = context.switchToHttp().getRequest(); const user = request.user as Principal | undefined; if (!user) throw new UnauthorizedException(); if (user.role === Role.SUPER_ADMIN) return true; const target = request.params.organizationId ?? request.body?.organizationId ?? user.organizationId; if (!target || target !== user.organizationId) throw new ForbiddenException('Organization access denied'); return true; } }
