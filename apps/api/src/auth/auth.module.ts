import { Module } from '@nestjs/common'; import { JwtModule } from '@nestjs/jwt'; import { TypeOrmModule } from '@nestjs/typeorm'; import { AuthController } from './auth.controller'; import { AuthService } from './auth.service'; import { User } from '../database/entities/user.entity'; import { Organization } from '../database/entities/organization.entity'; import { RefreshToken } from '../database/entities/refresh-token.entity'; import { AuditLog } from '../database/entities/audit-log.entity'; import { JwtAuthGuard } from './jwt-auth.guard'; import { RolesGuard } from './roles.guard'; import { OrganizationAccessGuard } from './organization-access.guard';
@Module({ imports: [JwtModule.register({}), TypeOrmModule.forFeature([User, Organization, RefreshToken, AuditLog])], controllers: [AuthController], providers: [AuthService, JwtAuthGuard, RolesGuard, OrganizationAccessGuard], exports: [JwtModule, JwtAuthGuard, RolesGuard, OrganizationAccessGuard] }) export class AuthModule {}
